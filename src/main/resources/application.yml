
spring:
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/likelion
      username: root
      password: USER_PASSWORD
    jpa:
      show-sql: true
      database-platform: org.hibernate.dialect.MySQL8Dialect
      database: mysql # ??? db ??
      hibernate.ddl-auto: update  # create ?? ??!!! create(drop+create)???!!
